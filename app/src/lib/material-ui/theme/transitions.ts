import { TransitionsOptions } from '@material-ui/core';

const transitions: TransitionsOptions = {
  duration: {
    shortest: 100,
    shorter: 100,
    short: 200,
    standard: 250,
    complex: 300,
    enteringScreen: 300,
    leavingScreen: 300,
  },
};

export default transitions;
